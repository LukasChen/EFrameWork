# EFrameWork

Unity game framework repository.

## Packages

- `packages/com.eframework.core`: initial reusable Unity framework package extracted from CozyBloom.

## DevMemory

Framework-level reusable lessons are archived under `memory/`.
